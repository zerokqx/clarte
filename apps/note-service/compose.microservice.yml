services:
  clarte-note-service:
    image: apps-note-service:latest
    env_file: ".env"
    environment:
      PORT: 3000
    networks:
      - default

networks:
  default:
    external: true
    name: clarte-network
