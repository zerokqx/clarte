syntax = "proto3";
import "google/protobuf/empty.proto";
package notes;

service NotesService {
  rpc CreateNote(CreateNoteRequest) returns (google.protobuf.Empty);
  rpc GetBytes(GetBytesRequest) returns (GetBytesResponse);
  rpc GetNoteById(GetBytesRequest) returns (Note);
}

message Note {
  string id = 1;
  repeated string tags = 2;
  string createdAt = 3;
  string updatedAt = 4;
}
message GetNoteByIdRequest {
  string id = 1;
}

message GetBytesRequest {
  string id = 1;
}

message GetBytesResponse {
  bytes bytes = 1;
}

message CreateNoteRequest {
  string text = 1;
  repeated string tags = 2;
  bytes bytes = 3;
}
