services:
  clarte-notification-service:
    image: apps-notification-service:latest
    container_name: clarte-notification-service
    build:
      context: ../..
      dockerfile: Dockerfile
      args:
        - SERVICE_NAME=notification-service
    env_file:
      - .env
      - .env.docker
    networks:
      - default
      - notification-net
networks:
  default:
    external: true
    name: clarte-network
  notification-net:
    external: true
    name: clarte-notification-network
