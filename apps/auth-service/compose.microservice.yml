services:
  clarte-auth-service:
    image: apps-auth-service:latest
    container_name: clarte-auth-service
    build:
      context: ../..
      dockerfile: apps/auth-service/Dockerfile
      args:
        - SERVICE_NAME=auth-service
    env_file:
      - .env
      - .env.docker
    networks:
      - default
networks:
  default:
    external: true
    name: clarte-network
